spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bazarya
    username: bazarya
    password: bazarya
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
  thymeleaf:
    cache: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  seo:
    base-url: https://example.com
